import React from "react";

const Home = () => {
    let foo: string = "React";
    const bar: string = "TypeScript";

    return (
        <>
            <h1>
                Hello World!!!
            </h1>
            <p>
                {foo} + {bar} + Laravel
            </p>
        </>
    );
};

export default Home;
